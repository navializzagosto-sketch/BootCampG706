😶‍🌫️ `git --version`-> me sirve para consutar la verdion del git  
`cd` --> me sirve para navergar entre carpetas   
`git init` -> para inicializar el repositorio web  
`git status` -> para verificar estado de archivos  
`git add` . -> para agregar los archivos a mi git como a la caja  
`git commit -m " first commit"` -> para sellar la caja y colocarle un comentario   
`git config --global user.email` "navializz.agosto@gmail.com" configurar el correo  
`git config --global user.name`"Elizabeth Navia Mejia" configurar el nombre  
`git remote add origin` -> https://github.com/fernandogallegoh75/BootCampProfesG706.git agrego la ruta de origen del repositorio git mio  
`git clone` ->https://github.com/fernandogallegoh75/BootCampProfesG706.git para clonar unrepositorio en este caso me clone el de los orofes   
`😶‍🌫️ `git --version`-> me sirve para consutar la verdion del git  
`cd` --> me sirve para navergar entre carpetas   
`git init` -> para inicializar el repositorio web  
`git status` -> para verificar estado de archivos  
`git add` . -> para agregar los archivos a mi git como a la caja  
`git commit -m " first commit"` -> para sellar la caja y colocarle un comentario   
`git config --global user.email` "navializz.agosto@gmail.com" configurar el correo  
`git config --global user.name`"Elizabeth Navia Mejia" configurar el nombre  
`git remote add origin` -> https://github.com/fernandogallegoh75/BootCampProfesG706.git agrego la ruta de origen del repositorio git mio  
`git clone` ->https://github.com/fernandogallegoh75/BootCampProfesG706.git para clonar unrepositorio en este caso me clone el de los orofes   
`Set-ExecutionPolicy -ExecutionPolicy Unrestricted -Scope CurrentUser` -> debe ejecutar dentro de powershell como administrador y sirve para habilitar ejecución de comandos git add  . para agregar los archivos a mi git (caja)  
` ` `   -> una sola comilla la abre y la cierra para subrrallar 
`python --version`--> nos sirve para verificar la version
`Python -m venv env312` -> crear un entorno virtual en python 
`dir` -> para consultar directorio
activarlo `` env312\scripts\activate ``` -> debe ejecutar dentro de powershell como administrador y sirve para habilitar ejecución de comandos   
` ` `   -> una sola comilla la abre y la cierra para subrrallar 
`python --version`--> nos sirve para verificar la version
`Python -m venv env312` -> crear un entorno virtual en python 
`dir` -> para consultar directorio
activarlo `` env312\scripts\activate ``
`pip list` -> lista de libreria de python (instalador)
##librerias de python
`python.exe -m pip install --upgrade pip` 
`archivo .gitignore` -> nos ayuda todo lo que queremos ignorar como en este caso el programa python en el caso del estudiante en ese archivo se coloca el nombrede la carpeta `mision1/env312`
`deactivate` -> para desactivar el entorno
`pip install pandas numpy matplotlib`
pandas -> manejo de datos `pip install pandas`
numy -> calculo matematico `pip install pandas numpy`
matplolib ->graficos `pip install matplotlib`
`as` es para minimizar arias ejemplo pandas pd
cuando veo una `({})` en python es porque es un diccionario de datos cada coma , que se le coloque representa los elementos del diccionario, 0para la vase de datos debe coincidir los elementos de ejemplo nombre igual que los de edad  
`#`para otros programas de diseño web # es para titulos pero para python algo que quiero comentar pero que no son ejecutables con la caracteristicas de que se visualiza mejor en la web